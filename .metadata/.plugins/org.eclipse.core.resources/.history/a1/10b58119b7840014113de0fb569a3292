package logic;

public class Logic {

	private Car[] carList;
	private Buffer buffer;
	private Parser parser;
	
	public void supply(){
		
		while(buffer.isAnyOrder()){
			for(int i = 0; i < parser.getNCars(); i++){
				carList[i].start();
			}
		}
	}
	
}
