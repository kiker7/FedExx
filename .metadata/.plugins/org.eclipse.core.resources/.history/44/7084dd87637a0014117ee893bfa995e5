package logic;

import io.MapRead;
import io.OrderRead;

import java.io.File;

import data_structure.Heap;
import dijkstra.Map;

public class Parser {

	private Map mapa;
	private Heap<Order> priorityList;
	private File mapFile, orderFile;
	private int nCars, nPackInCar;
	private MapRead mReader;
	private OrderRead oReader;
	
	public void parser(String[] args){
		for(int i = 0; i < args.length ; i++){
			switch(i) {
				case 0: mapa = mReader.mapRead(new File())
					
				
			}
		}
	}
}
